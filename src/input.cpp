#include <stdio.h>
int main(){
    int x = 5;
    float y = x + 10;
    if (x < 5)
        z= y + 1;
    
    while (x > 0)
        x = x - 1;
    
    for (x = 0; x < 10; x=x+1)
        y = y + x;
}
